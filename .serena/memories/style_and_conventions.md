Style/conventions:
- Python scripts with argparse CLIs and JSON output.
- Snake_case functions, ALL_CAPS for constants.
- Docstrings on functions, minimal inline comments.
- Uses pathlib.Path and shared FFmpegWrapper for ffmpeg operations.
- Environment variables for API keys (GEMINI_API_KEY, HUGGINGFACE_TOKEN).